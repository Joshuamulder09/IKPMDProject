<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.joshu.quiz">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyFirebaseApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".OverOnsActivity" />
        <activity android:name=".SpelregelsActivity" />
        <activity android:name=".QuizActivity" />
        <activity android:name=".ResultActivity" />
        <activity android:name=".ChangeQuestionsActivity" />
        <activity android:name=".NewQuestionActivity" />
        <activity android:name=".QuestionDetailsActivity" />
        <activity android:name=".deleteQuestionActivity" />
        <activity android:name=".RateAppActivity"></activity>
    </application>

</manifest>